# 通宵自主迭代计划

_2026-09-09 22:xx 制定　用户授权："set to iterate and debug for tonight, hope tomorrow we
will see some results"_

## 授权范围

在下列队列内，**发现问题自主修复、结果出来自主推进下一步，不必每步都问**。
仅在遇到**改变论文核心结论或方向**的决定性分歧时才停下来问（沿用此前 EXP-R0~R3 的
处理方式：污染 bug 发现即修即报，但方向性判断——如"要不要推翻现有故事线"——仍会等你）。
每一步完成都会 `git commit` + `push`，你随时能看进度，不必等到早上才看得到东西。

## 当前状态（起点）

- S9（跨模型迁移）、S10（4 跳鲁棒性）已解决，结果正面：学习式读出跨模型更强、
  4 跳内无显著衰减、对抗性 guard_lexicon 规避无效。
- **S12 进行中**（作业 20037356）：把跳数从 4 扩展到 10，找真实衰减上限（若存在）。
  复用 hop0-4 checkpoint，只生成 hop5-10，预计约 2.5h。

## 今夜队列（按依赖顺序，遇阻可跳过后续项先做不依赖的）

1. **等 S12 完成** → 分析完整 0-10 跳曲线：
   - 若 10 跳内仍不衰减 → 记录"在测试范围内视界不存在"为强结论，**不再无限加跳**
     （避免无意义烧算力），转入队列项 2。
   - 若出现衰减 → 拟合衰减形状（沿用 EXP-002 的 geometric vs floor 模型选择），
     标出转折跳数。
   - 无论哪种，都写入 ledger 并 push。

2. **utility 不掉点主表**（CPU，快，优先做——这是经验门槛里最便宜也最必要的一项）：
   用已有数据（EXP-R1/R2/R3 的 `gold` 字段）比较 triggered vs clean 的答案正确率。
   若发现 trigger 注入损害任务准确率，如实记录（这本身也是重要发现，不能因为想要
   好看的结果就回避）。

3. **第二数据集**（GPU，中等规模）：本地缓存已有 `MATH-500`。用已验证干净的管线
   （Tulu-3-8B 生成 + Qwen3-14B 中继）跑一轮检测/归因，确认核心发现（检测有效但
   朴素读出等同随机、学习式读出显著恢复）是否跨数据集稳健。这是经验门槛里
   "3 数据集"的第 2 项。

4. **第二 embedding 交叉验证**（CPU，快）：用 gte-base 复核 EXP-R2 v2 / EXP-R3
   v2（10 跳版）的核心数字，确认不依赖单一打分器（沿用 EXP-R1c 的方法论）。

5. **若时间允许——升级对抗攻击**：单项 guard_lexicon 已证明无效，用此前记录的
   Guard-Lexicon + Do-Not-Mention + Monitor-Aware 叠加压力版本重测，看更强的攻击
   能否真正破坏签名。若仍然无效，这是一个更有说服力的鲁棒性主张；若能破坏，
   则得到真实的攻防边界，同样是有价值的结果。

## 资源纪律

- 全程只占 1 张 GPU（`ihc` account），不与用户已有作业抢资源。
- 每个 GPU 作业提交前先确认没有沿用被污染的默认参数（`enable_thinking=False` +
  `strip_think` 防御层已是默认，新脚本若涉及生成必须复用 `gen_and_relay.py` 的
  `build_chat`/`batched_generate`，不得重新手写生成逻辑）。
- 每步结果无论好坏都如实记录到 `experiment_ledger.md` 并 push，不因为结果不好看
  就少记或晚记。

## 提前声明的止损点

- 若连续两步遇到与本次 think-block 同等级的方法学事故（即结果被基础设施问题
  污染而非真实发现），**停止继续排队新实验**，转为在 `.pipeline/docs/` 下写一份
  事故清单等用户醒来定夺，而不是自己不断加修补层。
- 单个 GPU 作业若运行超过其预设时限的 1.5 倍仍未完成，视为异常，记录并跳过，
  不无限等待。
